{
  "name": "animated-slide-in-background",
  "version": "1.0.9",
  "description": "Adds a slide in background effect to your DOM elements",
  "repository": "github:Richardinho/animated-slide-in-background",
  "main": "dist/main.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "webpack --config webpack.config.js",
    "clean-test": "rm -rf ./dist",
    "tsc-test": "tsc -p ./ts-config.test.json",
    "test": "npm run clean-test && npm run tsc-test && jest ./test-dir && npm run clean-test"
  },
  "author": "Richard Hunter",
  "license": "ISC",
  "dependencies": {
    "@types/jest": "^26.0.14"
  },
  "devDependencies": {
    "jest": "^26.4.2",
    "ts-loader": "^8.0.4",
    "typescript": "^4.0.3",
    "webpack": "^5.0.0",
    "webpack-cli": "^4.0.0"
  }
}
