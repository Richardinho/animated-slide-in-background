{
  "name": "animated-slide-in-background",
  "version": "1.0.0",
  "description": "",
  "main": "dist/main.js",
  "type": "module",
  "files": ["dist"],
  "scripts": {
    "clean-test": "rm -rf ./dist",
    "clean-build": "rm -rf ./dist",
    "tsc-test": "tsc -p ./ts-config.test.json",
    "tsc-prod": "tsc -p ./ts-config.prod.json",
    "dev": "tsc",
    "test": "npm run clean-test && npm run tsc-test && jest ./test-dir && npm run clean-test",
    "build": "npm run clean-build && npm run tsc-prod"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/jest": "^26.0.14"
  },
  "devDependencies": {
    "jest": "^26.4.2"
  }
}
