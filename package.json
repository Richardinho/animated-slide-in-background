{
  "name": "animated-slide-in-background",
  "version": "1.0.0",
  "description": "",
  "main": "tsc-output/main.js",
  "type": "module",
  "scripts": {
    "clean-test": "rm -rf ./test-dir",
    "clean-build": "rm -rf ./tsc-output",
    "minify": "java -jar closure-compiler-v20200927.jar --js_output_file=out.js 'tsc-output/**.js'",
    "tsc-test": "tsc -p ./ts-config.test.json",
    "tsc-prod": "tsc -p ./ts-config.prod.json",
    "dev": "tsc",
    "test": "npm run clean-test && npm run tsc-test && jest ./test-dir && npm run clean-test",
    "build": "npm run clean-build && npm run tsc-prod && npm run minify"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/jest": "^26.0.14"
  },
  "devDependencies": {
    "jest": "^26.4.2"
  }
}
